apiVersion: v2
name: app5-fileservice
description: >-
  Multi-cloud FastAPI demo service (app5-fileservice). Same image and chart
  deploy against any Kubernetes cluster - AWS EKS, Azure AKS, or local kind -
  with only CLOUD_PROVIDER changing per values file.
type: application
version: 0.1.0
appVersion: "1.0.0"
